/** @type {import('tailwindcss').Config} */
const config = {
  content: ["./src/**/*.{js,ts,jsx,tsx}"],
  darkMode: "class", // <-- add this
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
